[workspace]
members = [
    "src-core",
    # "src-tauri"  <-- Comment this out for Phase 1
]
resolver = "2"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true

[profile.dev]
opt-level = 1